# PII Shield - Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core PII/PHI Detection ──────────────────────────────────────────────────
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# ── spaCy NLP (must also download a model — see setup.sh) ──────────────────
spacy>=3.7.0

# ── Document Parsers ────────────────────────────────────────────────────────
pdfplumber>=0.10.0          # PDF extraction
python-docx>=1.1.0          # Word documents (.docx)
pandas>=2.0.0               # Excel / CSV
openpyxl>=3.1.0             # Excel engine for pandas
